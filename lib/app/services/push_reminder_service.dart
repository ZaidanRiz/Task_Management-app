// Push reminders are disabled in the free/local-only setup.
// This stub exists only temporarily so any stale imports won't break the build.

class PushReminderService {
  PushReminderService._();
  static final PushReminderService instance = PushReminderService._();
}
